import { StickyActionFooter } from './StickyActionFooter';

export const StickyActionFooterExamples = () => (
  <StickyActionFooter
    caption="Recipe footers give prototypes a bottom action treatment closer to a real screen shell."
    primaryAction={{ label: 'Confirm' }}
    surface="sheet"
  />
);
